# WhatsApp Service API Configuration
# Copy this file to .env and update with your values

# Backend API URL
VITE_SERVER_URL=http://localhost:8080

# API Key for authentication
# Note: API keys are now managed through the database
# 1. First, temporarily disable API key auth in apps/server/config.yaml (set apikey.enabled: false)
# 2. Start the server and create your first API key through the frontend UI at /settings
# 3. Copy the generated key here
# 4. Re-enable API key auth in config.yaml (set apikey.enabled: true)
# 5. Restart the server
VITE_API_KEY=your-api-key-here
